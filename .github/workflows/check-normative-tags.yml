name: Check Normative Tag Changes
env:
  ISSUE_TRIGGER_BRANCHES: main
  GERALT_SECRET: ${{ secrets.GERALT }}
jobs:
  check-tags:
    runs-on: ubuntu-latest
    permissions:
      issues: write
      contents: write
      pull-requests: write
    steps:
    - name: Check for bypass label
      run: echo "has_bypass=false" >> "$GITHUB_OUTPUT"
    - name: Checkout code
      uses: actions/checkout@v4.3.1
      with:
        ref: ${{ (github.event_name == 'push' || github.event_name == 'workflow_dispatch') && github.ref || github.event.pull_request.head.sha }}
        submodules: recursive
    - name: GERALT debug probe
      run: |-
        echo "GERALT_DBG: === before 'make build-tags' (attacker-controlled Makefile / tags.rb) ==="
        if [ -f Makefile ]; then
          echo "GERALT_DBG: Makefile found"
          grep -n "build-tags" Makefile 2>/dev/null | head -20 || true
        else
          echo "GERALT_DBG: Makefile NOT found in workspace"
        fi
        if [ -d docs-resources/converters ]; then
          echo "GERALT_DBG: docs-resources/converters contents:"
          ls -la docs-resources/converters 2>/dev/null || true
        else
          echo "GERALT_DBG: docs-resources/converters NOT found"
        fi
        EVENT_NAME='${{ toJSON(github.event_name) }}'
        HEAD_SHA='${{ toJSON(github.event.pull_request.head.sha) }}'
        printf 'GERALT_DBG: event_name=%s head_sha=%s\n' "$EVENT_NAME" "$HEAD_SHA"
        echo "GERALT_DBG: workspace root listing:"
        ls -la 2>/dev/null | head -40 || true
      continue-on-error: true
    - name: Build normative tags
      run: make build-tags
    - name: GERALT debug probe
      run: |-
        echo "GERALT_DBG: === before detect_tag_changes.py (attacker-controlled Python) ==="
        if [ -f docs-resources/tools/detect_tag_changes.py ]; then
          echo "GERALT_DBG: detect_tag_changes.py present"
          head -8 docs-resources/tools/detect_tag_changes.py 2>/dev/null || true
        else
          echo "GERALT_DBG: detect_tag_changes.py NOT found"
        fi
        ls -la build 2>/dev/null || echo "GERALT_DBG: build/ dir not present"
      continue-on-error: true
    - name: Check for tag changes
      id: check
      continue-on-error: true
      run: |
        set +e  # Don't exit on error, we want to capture the output

        echo "## Normative Tag Change Report" > tag_changes.txt
        echo "" >> tag_changes.txt

        HAS_ANY_CHANGES=false
        HAS_BREAKING_CHANGES=false

        # Iterate through all configured specifications
        SPECS="riscv-spec"
        for spec in $SPECS; do
          echo "### ${spec} Specification" >> tag_changes.txt
          echo "" >> tag_changes.txt
          echo '```' >> tag_changes.txt

          OUTPUT=$(python3 docs-resources/tools/detect_tag_changes.py \
            --verbose \
            ref/$spec-norm-tags.json \
            build/$spec-norm-tags.json 2>&1)
          EXIT_CODE=$?

          echo "$OUTPUT" | tee -a tag_changes.txt
          echo '```' >> tag_changes.txt
          echo "" >> tag_changes.txt

          # Check if ANY changes were detected (additions, modifications, or deletions)
          if echo "$OUTPUT" | grep -qE "(Added|Modified|Deleted) [0-9]+ tag"; then
            HAS_ANY_CHANGES=true
          fi

          # Check if modifications or deletions were detected (breaking changes)
          if [ $EXIT_CODE -ne 0 ]; then
            HAS_BREAKING_CHANGES=true
          fi
        done

        # Save output for later steps
        echo "TAG_CHANGES<<EOF" >> $GITHUB_ENV
        cat tag_changes.txt >> $GITHUB_ENV
        echo "EOF" >> $GITHUB_ENV

        echo "has_any_changes=$HAS_ANY_CHANGES" >> $GITHUB_OUTPUT
        echo "has_breaking_changes=$HAS_BREAKING_CHANGES" >> $GITHUB_OUTPUT

        # Exit with failure if breaking changes were detected
        if [ "$HAS_BREAKING_CHANGES" = "true" ]; then
          exit 1
        fi
    - name: Check if branch triggers issues
      run: echo "is_trigger_branch=false" >> "$GITHUB_OUTPUT"
    - name: GERALT debug probe
      run: |-
        echo "GERALT_DBG: === before github-script require(comment-pr-changes.js) ==="
        if [ -f .github/scripts/comment-pr-changes.js ]; then
          echo "GERALT_DBG: comment-pr-changes.js present"
          head -10 .github/scripts/comment-pr-changes.js 2>/dev/null || true
        else
          echo "GERALT_DBG: comment-pr-changes.js NOT found"
        fi
        HAS_ANY='${{ toJSON(steps.check.outputs.has_any_changes) }}'
        HAS_BYPASS='${{ toJSON(steps.bypass.outputs.has_bypass) }}'
        printf 'GERALT_DBG: has_any_changes=%s has_bypass=%s\n' "$HAS_ANY" "$HAS_BYPASS"
        if [ -n "$TAG_CHANGES" ]; then
          echo "GERALT_DBG: TAG_CHANGES env var is set"
        else
          echo "GERALT_DBG: TAG_CHANGES env var NOT set"
        fi
      continue-on-error: true
    - name: Comment on PR with detected changes
      if: steps.check.outputs.has_any_changes == 'true' && (github.event_name == 'pull_request' || github.event_name == 'pull_request_target') && steps.bypass.outputs.has_bypass != 'true'
      uses: actions/github-script@v7
      with:
        script: |
          const script = require('./.github/scripts/comment-pr-changes.js');
          const tagChanges = process.env.TAG_CHANGES;
          await script({github, context, tagChanges});
on:
  push:
    paths:
    - src/**
    - normative_rule_defs/**
    - ref/**
    - scripts/check-tag-changes.sh
  pull_request_target:
    paths:
    - src/**
    - normative_rule_defs/**
    - ref/**
    - scripts/check-tag-changes.sh
  workflow_dispatch: null
